app-id: io.github.nuttyartist.notes
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: notes

finish-args:
  - --device=dri
  - --share=ipc
  - --share=network
  - --socket=x11
  - --talk-name=org.kde.StatusNotifierWatcher

modules:
  - name: notes
    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DUPDATE_CHECKER=OFF
      - -DPRO_VERSION=OFF
    sources:
      - type: git
        url: https://github.com/nuttyartist/notes.git
        tag: v2.3.1
        commit: 0b13300e3b7045ca20dab2173441bee59ed469aa
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)$
      - type: patch
        paths:
          # https://github.com/nuttyartist/notes/commit/d7d781ee1dd9fd8670ae7f4ea5fc798bcb0acb2a
          - patches/0001-Fix-730-by-adding-this-to-createWindowContainer-calls.patch
          # https://github.com/nuttyartist/notes/commit/1585918af82e9ab03208be00a20816e5599f93f2
          - patches/0002-build-Search-for-private-modules-with-Qt-6.10.patch
